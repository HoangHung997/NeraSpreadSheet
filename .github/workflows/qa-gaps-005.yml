name: QA gaps 005 — Avalonia interaction and Table/Filter

on:
  push:
    branches: [fix/qa-gaps-005]
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: qa-gaps-005-${{ github.ref }}
  cancel-in-progress: true

jobs:
  core-and-hosts:
    uses: ./.github/workflows/ci.yml
  openxml:
    uses: ./.github/workflows/q003c-openxml-analytics-gate.yml
  avalonia:
    uses: ./.github/workflows/avalonia-primary-ribbon-002.yml
