# Function Extension SDK and Conditional Aggregates — work in progress

This marker reserves the next implementation batch on the active feature branch. It will be replaced by the validated handoff after implementation and exact-head CI pass.
