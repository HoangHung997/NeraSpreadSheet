# Dynamic Arrays Foundation contract

The dynamic subsystem contains 20 unique public names after F012. F012 adds `TOCOL`, `TOROW`, `TRIMRANGE`, `VSTACK`, `WRAPCOLS` and `WRAPROWS`; `XMATCH`, `IFERROR`, `IFNA` and `SWITCH` expose array-aware bridges without being counted twice.

All outputs use immutable `FormulaArrayValue`, a 1,000,000-cell cap, atomic worksheet spill ownership and at most eight stabilization passes.

Total formula surface: **296 / at least 538 functions**.
