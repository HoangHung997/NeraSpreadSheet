# Dynamic Arrays Foundation contract

The dynamic subsystem contains 14 unique public names after F011. F011 adds `PIVOTBY`, `SORTBY` and `TAKE`; `OFFSET`, `ROW` and `ROWS` also expose dynamic spill bridges without being counted twice.

All outputs use immutable `FormulaArrayValue`, a 1,000,000-cell cap, atomic worksheet spill ownership and at most eight stabilization passes.

Total formula surface: **286 / at least 538 functions**.
